let mixer = mixitup(".wrapper");

$(document).ready(function () {

    $(".slider").slick({
      slidesToShow: 3,
      arrows: true,
      prevArrow: ".leftArrows",
      nextArrow: ".rightArrows",
      centerPadding: "0px",
    });
  
  });